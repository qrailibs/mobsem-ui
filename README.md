# mobsem-ui

Mobile Semantic HTML UI library.

# Progress

**functional**

-   [x] `<Holdable />` functional component (showing a menu on long hold)
-   [] `<Sheet />` functional component (mobile modal showen from bottom withd drag handle)

**semantical**

-   [x] `<Caption />`
-   [x] `<Button />` functional component (click ui feedback)
-   [x] `<Checkbox />` functional component (click with ui feedback & vibration)
-   [x] `<Divider />`
-   [x] `<List />` and `<ListItem />`
-   [x] `<Popover />`
-   [x] `<Scrollable />`
    -   [x] custom scrollbars
    -   [x] custom overscroll behavior, like on ios
    -   [x] custom overscroll gradient on corners
-   [ ] `<Alert />`
